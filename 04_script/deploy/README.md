# Deploy

Place environment-specific deploy notes and Docker/Compose files here (integer-prefixed subfolders if multiple targets).

Example (from S01): `gce/` with `Dockerfile`, `docker-compose.yml`, `Caddyfile`.
