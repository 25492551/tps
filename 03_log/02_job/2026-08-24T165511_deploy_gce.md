# Deploy GCE (bgp-001.com)

**When (UTC)**: 2026-08-24T165511

## Summary

Rebuilt and redeployed `gce-tps-1`, reloaded Caddy. Live `https://bgp-001.com` returns HTTP 200. Batch includes agent portal, UI/UX polish, DB migrations through `026`, and recent user-portal fixes.

## Changes

- Runtime: `04_script/deploy/gce` `docker compose up -d --build` + Caddy reload
