# Deploy GCE (bgp-001.com)

**When**: 2026-08-24T160610 UTC

## Summary

Rebuilt and redeployed `gce-tps-1`, reloaded Caddy. Live `https://bgp-001.com` returns HTTP 200; container healthy. Includes recent UI/API work (deposit charts, agent create reject, wallet/key UX, toggles, table scroll, bank-requests header, etc.).

## Changes

- Runtime: `04_script/deploy/gce` `docker compose up -d --build` + Caddy reload
