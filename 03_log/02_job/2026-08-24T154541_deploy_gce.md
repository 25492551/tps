# Deploy GCE: achromatic UI + rates/chrome polish

**When**: 2026-08-24T154541 UTC

## Summary

Rebuilt and redeployed `gce-tps-1` (`docker compose up -d --build`), reloaded Caddy. Live `https://bgp-001.com` returns HTTP 200; container healthy.

## Changes

- Deploy only (image rebuild includes recent web CSS/UI: grayscale palette, fonts, rates table, shadows, topbar)
