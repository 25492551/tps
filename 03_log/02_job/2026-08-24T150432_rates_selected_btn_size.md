# Rates: match 선택 / 선택됨 button size

**When**: 2026-08-24T150432 UTC

## Summary

`선택됨` is a disabled primary button (same padding/height/font as `선택`) with shared `min-width` so the action cell size does not jump.

## Changes

- `04_script/apps/web/src/portals/admin/pages.tsx`
- `04_script/apps/web/src/styles.css`
