# Deploy TPS to GCE (bgp-001.com)

**When**: 2026-08-24T122415 UTC

## Summary

Rebuilt `gce-tps-1` (`tps-app:latest`) and reloaded Caddy. Live site serves the achromatic serif theme and `/admin-login` with captcha. Health 200; `/api/auth/login` as admin returns the admin-login redirect error.

## Changes

- Runtime only: `docker compose up -d --build` in `04_script/deploy/gce/`
